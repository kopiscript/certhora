DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Certificate delivery email (Resend)
RESEND_API_KEY=""
SMTP_FROM="Certhora <no-reply@example.com>"

# Secures the Vercel Cron endpoint at /api/cron/send-emails — must match the
# value Vercel sends as `Authorization: Bearer <CRON_SECRET>`
CRON_SECRET=""
